"""Standalone HDFC banking chatbot + embeddings service (owner: Harshvardhan Lokhande).

Everything in this package is self-contained under ``chatbot/``.  The
existing ``Python/ai_engine`` is consumed read-only through a thin
integration adapter (``chatbot.app.integrations``) and is never copied
or modified here.
"""
