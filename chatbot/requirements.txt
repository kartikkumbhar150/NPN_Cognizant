# Standalone chatbot service dependencies (chatbot/ only — shared deps of
# Python/requirements.txt are intentionally NOT modified for this module).
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
pandas>=2.0
qdrant-client>=1.9
fastembed>=0.3
python-dotenv>=1.0
# Required transitively to import the existing read-only ai_engine
# (ai_engine/data_loader.py imports sqlalchemy at module import time).
sqlalchemy>=2.0

# Tests
pytest>=8.0
httpx>=0.27
