# Chatbot API Routes

## Purpose

This directory contains HTTP route definitions for the chatbot service.

## Contents

| Item | Description |
| --- | --- |
| `router.py` | Defines `/chat`, `/ask`, and `/health` endpoints and maps service exceptions to HTTP responses. |
| `__init__.py` | Marks the directory as a Python package. |

## Operational Notes

Use this directory as part of the documented application workflow. Keep generated files, secrets, and environment-specific artifacts out of source control unless they are intentional sample data.
